@model Barrway.DTO.MarketplaceModels.MarketplaceCompanyModel

<!-- heading -->
@*<section class="heading company-detail-hed">
    
    <div class="container-fluid pd-left pd-right">
        <div class="row">
            <div class="col-md-12">
                <div class="main-banner">
                    <div class="main-banner-inner">
                        <div class="main-banner-inner-content">
                            <p class="banner-text-content">Company name english (Company name Chinese)</p>
                            <button class="banner-button">Book Now</button>
                        </div>
                    </div>
                </div>
                <div class="head">
                        <h2>@(string.IsNullOrEmpty(Model.COMPANY_NAME_ENGLISH)? "Company Name" : Model.COMPANY_NAME_ENGLISH) (@Model.COMPANY_NAME_CHINESE)</h2>
                    </div>
            </div>
        </div>
    </div>
</section>*@
<!-- heading -->
<section class="company-detail-main templet-two">
    <div class="main-banner">
        <div class="main-banner-inner">
            <div class="main-banner-inner-content" style="background-image: url('@Model.COMPANY_BANNER_PATH.Replace("~", "")'); background-size: 100%; background-repeat: no-repeat; background-position: center center;">
                <p class="banner-text-content">@Model.COMPANY_NAME_ENGLISH (@Model.COMPANY_NAME_CHINESE)</p>
                <button class="banner-button">Book Now</button>
            </div>
        </div>
    </div>
    <div class="container">
        @*<div class="row">
                <div class="col-md-12">
                    <div class="cd-banner">
                        <img onerror="this.src='../assets/marketplace/image/marketplace-detail.jpg'" src="@Model.COMPANY_BANNER_PATH.Replace("~", "..")" style="width: 100%; max-height:300px;">
                    </div>
                </div>
            </div>*@
        <div class="row mt-30">
            <div class="col-md-12">
                <div class="detail">
                    <p class="pink"><b>@Resource.Company_description (200 @Resource.words)</b></p>
                    <p>@(string.IsNullOrEmpty(Model.COMPANY_DESCRIPTION)? "Discription not mentioned": Model.COMPANY_DESCRIPTION)</p>
                </div>
            </div>
            <div class="col-md-12">
                <div class="detail">
                    <p class="pink"><b>Tag 1</b></p>
                    <p>
                        @if (Model.TAGs !=null)
                        {
                            
                            foreach (var item in Model.TAGs)
                            {
                                <a href="/Marketplace/Search?keyword=@item.value">@item.value,</a>
                            }
                        }

                    </p>
                </div>
            </div>
            <div class="col-md-12">
                <div class="detail">
                    <p class="pink"><b>@Resource.Company_category</b></p>
                    @if (Model.CALENDAR_CATEGORY_NAME_List != null)
                    {

                        foreach (var item in Model.CALENDAR_CATEGORY_NAME_List)
                        {
                            <p>@item.CALENDAR_CATEGORY_NAME</p>
                        }
                    }
                    else
                    {
                        <p>Sub-Category not mentioned</p>
                    }
                    @*<p>@(string.IsNullOrEmpty(Model.COMPANY_CATEGORY_NAME) ? "Category not mentioned": Model.COMPANY_CATEGORY_NAME)</p>*@
                </div>
            </div>
            <div class="col-md-12">
                <div class="detail">
                    <p class="pink"><b>@Resource.Company_sub_category</b></p>
                    @if (Model.CALENDAR_SUB_CATEGORY_NAME_List != null)
                    {

                        foreach (var item in Model.CALENDAR_SUB_CATEGORY_NAME_List)
                        {
                            <p>@item.CALENDAR_SUB_CATEGORY_NAME</p>
                        }
                    }
                    else
                    {
                        <p>Sub-Category not mentioned</p>
                    }

                    @*<p>@(string.IsNullOrEmpty(Model.COMPANY_SUB_CATEGORY_NAME) ? "Sub-Category not mentioned": Model.COMPANY_SUB_CATEGORY_NAME)</p>*@
                </div>
            </div>
            <div class="col-md-12">
                <div class="detail">
                    <p class="pink"><b>@Resource.Address</b></p>
                    <p>@(string.IsNullOrEmpty(Model.COMPANY_ADDRESS) ? "Address not mentioned": Model.COMPANY_ADDRESS)</p>
                </div>
            </div>
            <div class="col-md-12">
                <div class="add-phone">
                    <p class="pink"><b>@Resource.Phone</b></p>
                    <p>@(string.IsNullOrEmpty(Model.COMPANY_PHONE)? "--": Model.COMPANY_PHONE)</p>
                </div>
                <div class="add-email">
                    <p class="pink"><b>@Resource.Email</b></p>
                    <p>@(string.IsNullOrEmpty(Model.COMPANY_EMAIL)? "--": Model.COMPANY_EMAIL)</p>
                </div>
                <div class="add-social">
                    <p>
                        <a href="@(string.IsNullOrEmpty(Model.WECHAT_URL)? "#": Model.WECHAT_URL)"><img src="~/assets/marketplace/image/whatsapp.png"></a>
                        <a href="@(string.IsNullOrEmpty(Model.INSTAGRAM_URL)? "#": Model.INSTAGRAM_URL)"><img src="~/assets/marketplace/image/insta.png"></a>
                        <a href="@(string.IsNullOrEmpty(Model.FACEBOOK_URL)? "#": Model.FACEBOOK_URL)"><img src="~/assets/marketplace/image/facebook.png"></a>
                        <a href="@(string.IsNullOrEmpty(Model.WECHAT_URL)? "#": Model.WECHAT_URL)"><img src="~/assets/marketplace/image/chat.png"></a>
                    </p>
                </div>
                <div class="chat-button">
                    <button><img src="~/assets/marketplace/image/cahe-button.png" /><span>Chat</span></button>
                </div>
            </div>



                @*<div class="col-md-12">
                    <div class="detail">
                        <p class="pink"><b>@Resource.Country</b></p>
                        <p>@(string.IsNullOrEmpty(Model.COMPANY_COUNTRY_NAME) ? "Country not mentioned": Model.COMPANY_COUNTRY_NAME)</p>
                    </div>
                </div>
                <div class="col-md-12">
                    <div class="detail">
                        <p class="pink"><b>@Resource.City</b></p>
                        <p>@(string.IsNullOrEmpty(Model.COMPANY_CITY_NAME) ? "City not mentioned": Model.COMPANY_CITY_NAME)</p>
                    </div>
                </div>
                <div class="col-md-12">
                    <div class="detail">
                        <p class="pink"><b>@Resource.District</b></p>
                        <p>@(string.IsNullOrEmpty(Model.COMPANY_DISTRICT_NAME) ? "District not mentioned": Model.COMPANY_DISTRICT_NAME)</p>
                    </div>
                </div>*@

        </div>
    </div>
    </div>

    <div class="" id="MarkerMap_1712646834218" style="width: 100%; height: 600px;">

    </div>
</section>


<script>
    var mapDiv = 'MarkerMap_1712646834218';
    var mapLatLng = 'MarkerMap_1712646834218';
    var mapLatitude = '22.302711';
    var mapLongitude = '114.177216';
    var Latitude="@Model.Latitude";
    var Longitude = "@Model.Longitude";


    console.log(@Model.Latitude+","+@Model.Longitude,"latlong");

    if (Latitude == "" || Longitude == "") {
        initGMap(mapDiv, mapLatitude, mapLongitude);
    }
    else {
        initGMap(mapDiv, Latitude, Longitude);
    }

    initGMap(mapDiv, Latitude, Longitude);

    function initGMap(mapDiv, mapLatitude, mapLongitude) {

        var latitude = parseFloat(mapLatitude);
        var longitude = parseFloat(mapLongitude);
        var latLng = { lat: latitude, lng: longitude };

        // Create map
        var map = new google.maps.Map(document.getElementById(mapDiv), {
            zoom: 15,
            center: latLng,
            mapTypeControl: true, // Show map type control
            mapTypeId: google.maps.MapTypeId.ROADMAP, // Default to roadmap view
            zoomControl: true, // Show zoom control
            scaleControl: true, // Show scale control
            streetViewControl: true // Show street view control
        });

        if (Latitude != "" || Longitude != "") {
            /* Create marker*/
            var marker = new google.maps.Marker({
                position: latLng,
                map: map,
                draggable: false // Make marker draggable
            });

            // Add event listener for marker dragend event
            google.maps.event.addListener(marker, 'dragend', function (event) {
                var newLat = event.latLng.lat();
                var newLng = event.latLng.lng();
                //$('#Latitude').val(newLat);
                //$('#Longitude').val(newLng);

            });
        }
    }








</script>
